{
  "name": "todoapp",
  "version": "1.0.0",
  "description": "```\r Goal\r To create an app that mimics a todo list.",
  "main": "scripts.js",
  "scripts": {
    "watch": "concurrently \"npm run serve\" \"babel src -d dist --presets=es2015 -ws\"",
    "build": "rm -rf dist && babel src -d dist --presets=es2015",
    "serve": "http-server",
    "babel": "babel src -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "concurrently": "^4.0.1",
    "http-server": "^0.11.1"
  }
}
